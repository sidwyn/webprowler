{
  "name": "webprowler",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source AI web agent Chrome extension",
  "type": "module",
  "scripts": {
    "dev": "node build.mjs --watch",
    "build": "node build.mjs",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "~5.9.0",
    "@types/chrome": "*"
  }
}
